pipeline {
  agent {
    label 'node'
  }
  stages {
    stage('code quality') {
      steps {
        echo 'code quality'
        sh 'env'
      }
    }
    stage('style checks') {
//       when {
//         branch 'main'
//       }
      steps {
        echo 'style checks'
      }
    }
    stage('unit tests') {
//       when {
//         branch 'main'
//       }
      steps {
        echo 'unit tests'
      }
    }
  }
}
